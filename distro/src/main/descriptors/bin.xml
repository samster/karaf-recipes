<assembly>

    <id>bin</id>

    <formats>
        <format>zip</format>
    </formats>

    <files>
        <file>
            <!-- Install the features descriptor into the system repository -->
            <source>${basedir}/target/classes/${artifactId}-features.xml</source>
            <outputDirectory>system/com/samstercode/karafrecipes/distribution/${artifactId}/${version}</outputDirectory>
            <destName>${artifactId}-${version}-features.xml</destName>
            <fileMode>0644</fileMode>
        </file>
    </files>

    <fileSets>

        <!-- Expanded Karaf Standard Distribution -->
        <fileSet>
            <directory>${setup.folder}/apache-karaf-${karaf.version}</directory>
            <outputDirectory>/</outputDirectory>
            <excludes>
                <exclude>**/demos/**</exclude>
                <!-- Must exclude custom Branding because Karaf loads the first branding jar that it finds. -->
                <exclude>lib/custom-branding.jar</exclude>
                <!-- Karaf comes with a README in deploy folder which throws warnings in log -->
                <exclude>deploy/*</exclude>
                <!-- All Karaf distributions take out the extra files when being distributed -->
                <exclude>LICENSE</exclude>
                <exclude>NOTICE</exclude>
                <exclude>README</exclude>
                <exclude>RELEASE-NOTES</exclude>
                <exclude>karaf-manual*.html</exclude>
                <exclude>karaf-manual*.pdf</exclude>
                <!-- Exclude all of the scripts that will be copied in the next blocks. -->
                <exclude>bin/*</exclude>
                <exclude>etc/users.properties</exclude>
                <!-- Exclude this because it is customized. -->
                <exclude>etc/org.apache.karaf.features.cfg</exclude>
            </excludes>
        </fileSet>

        <!-- Copy over linux scripts to get the correct file mode -->
        <fileSet>
            <directory>${setup.folder}/apache-karaf-${karaf.version}</directory>
            <outputDirectory>/</outputDirectory>
            <includes>
                <include>bin/admin</include>
                <include>bin/karaf</include>
                <include>bin/start</include>
                <include>bin/stop</include>
                <include>bin/shell</include>
                <include>bin/client</include>
            </includes>
            <lineEnding>unix</lineEnding>
            <fileMode>0755</fileMode>
        </fileSet>

        <!-- Console Branding -->
       <!-- <fileSet>
            <directory>${setup.folder}</directory>
            <includes>
                <include>tie-branding.jar</include>
            </includes>
            <outputDirectory>/lib/</outputDirectory>
        </fileSet>-->

        <!-- Dynamic Config Files -->
        <fileSet>
            <directory>target/classes/etc</directory>
            <outputDirectory>/etc</outputDirectory>
            <fileMode>0644</fileMode>
        </fileSet>

        <!-- Static Executable Files -->
        <fileSet>
            <directory>src/main/distribution/bin</directory>
            <outputDirectory>/bin</outputDirectory>
            <fileMode>0755</fileMode>
        </fileSet>

        <fileSet>
            <directory>src/main/distribution/etc</directory>
            <outputDirectory>/etc</outputDirectory>
            <fileMode>0644</fileMode>
        </fileSet>

    </fileSets>

</assembly>
